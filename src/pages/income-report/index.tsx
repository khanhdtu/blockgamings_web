import Box from '@material-ui/core/Box'
import { Layout } from '../../layouts'

export const IncomeReport = (): JSX.Element => {
  return (
    <Layout type='default'>
      <Box>IncomeReport</Box>
    </Layout>
  )
}

export default IncomeReport
